# `[ Strings : Challenges ]`

| `INSTRUCTIONS` |
|:-:|
| All assignment code should be written inside of the `main.py` file. |
| In VS Code use `CTRL + J` to open the terminal. |
| In the terminal type `python main.py` to execute your code. |
| In the terminal type `python test.py` to test your code for correctness. |


<br>

* `Preconditions` are requirements that must be true before a function is called in order for the function to operate correctly.

* `Constraints` are rules that tell you what you can or cannot do when solving a problem.

* `Requirements` are what the program is expected to be able to do. 

* `Hard coding` is the process of manually programming something that should be written programmatically.

<br>

---
<br>

# `Challenges`

### `Q1`
Print out the text `"ONE TWO THREE"` with a newline character between each word instead of a space.  
<br>

`Constraints`
- You must use a single print statement.  
- You must use a single string.  
- Spaces should be manually replaced by newline characters.  

---  
<br>

### `Q2`
Print out the file path --> `"C:\Users\USERNAME\Desktop"`  
**Hint:** Special Characters  
<br>

`Constraints`
- You must use a single print statement.  
- You must use a single string.  

---  
<br>

### `Q3`
Print out --> `"Where there's code. There's a bug."`  
**Hint:** Special Characters  
<br>

`Constraints`
- Include the quotation marks in the output.  
- You must use a single print statement.  
- You must use a single string.  

---  
<br>

### `Q4`
Pick any character and assign it to a variable named `character`.  

Using a built-in function, convert `character` into its integer value and assign it to `int_value`.  
* Print out the value of `int_value`.  

<br>

`Constraints`
- You must use only a single statement (one line) to convert and assign.  
- You must use a built-in function.  

---  
<br>

### `Q5`
Using built-in functions, convert `int_value` from Q4 into binary and assign it to `bin_value`.  
Print out the value of `bin_value`.  
<br>

`Constraints`
- You must use only a single statement (one line).  
- You must use a built-in function.  

---  
<br>

### `Q6`
Using built-in functions, convert `int_value` from Q4 into hexadecimal and assign it to `hex_value`.  
Print out the value of `hex_value`.  
<br>

`Constraints`
- You must use only a single statement (one line).  
- You must use a built-in function.  

---  
<br>

### `Q7`
Using built-in functions, convert `int_value` from Q4 back into a character and assign it to `char_value`.  
Print out the value of `char_value`.  
<br>

`Constraints`
- You must use only a single statement (one line).  
- You must use a built-in function.  

---  
<br>

### `Q8`
Define three variables: `first`, `second`, and `third`, and assign each a string literal of your choice.  
Write a code statement that concatenates `first` and `second` with a single space in between. Print the result.  
<br>

`Constraints`
- You must use a single print statement.  
- You must use concatenation.  
- The space must be added manually.  

---  
<br>

### `Q9`
Duplicate `third` 10 times and print the result.  
<br>

`Constraints`
- You must use a single print statement.  
- You must use duplication in your answer.  

---  
<br>

### `Q10`
Create a variable named `text` and assign it a string literal.  
Concatenate `"abcde"` with `text` using compound assignment.  
<br>

`Constraints`
- Use `text` only once when concatenating and assigning.  
- You must concatenate and assign in the same statement.  

---  
<br>

### `Q11`
Create a variable named `strang` and assign it a string literal.  
Duplicate `strang` 5 times using compound assignment.  
<br>

`Constraints`
- Use `strang` only once when duplicating and assigning.  
- You must duplicate and assign in the same statement.  

---  
<br>

### `Q12`
Write an expression to check if `"hello"` and `"Hello"` are equal. Print the result.  
<br>

`Constraints`
- Use only one print statement.  
- The expression must be written inside the print statement.  

---  
<br>

### `Q13`
Create variables `alpha`, `beta`, and `gamma` and assign each a string literal.  
Write an expression to check if `alpha` and `beta` are NOT equal. Print the result.  
<br>

`Constraints`
- Use only one print statement.  
- The expression must be written inside the print statement.  

---  
<br>

### `Q14`
Write an expression to check if `beta` is lexicographically greater than `gamma`. Print the result.  
<br>

`Constraints`
- Use only one print statement.  
- The expression must be written inside the print statement.  

---  
<br>

### `Q15`
Write an expression to check if `beta` is lexicographically less than `gamma`. Print the result.  
<br>

`Constraints`
- Use only one print statement.  
- The expression must be written inside the print statement.  

---  
<br>

### `Q16`
Define a variable named `alphabet` and assign it the value `"ABCDEFGHIJKLMNOPQRSTUVWXYZ"`  
Print out the 4th character in `alphabet` using subscripting.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use subscripting.  
- Use a single print statement.  

---  
<br>

### `Q17`
Print the 3rd-to-last character in `alphabet` using a negative index.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use subscripting.  
- Use a negative index.  
- Use a single print statement.  

---  
<br>

### `Q18`
Print a substring of `alphabet` that contains the first 7 characters using slicing.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use slicing notation.  
- Use positive indexes.  
- Use default values if applicable.  
- Use a single print statement.  

---  
<br>

### `Q19`
Print a substring of `alphabet` that contains the last 8 characters using slicing.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use slicing notation.  
- Use negative indexes.  
- Use default values if applicable.  
- Use a single print statement.  

---  
<br>

### `Q20`
Print a substring of `alphabet` that contains only characters 11 through 17 inclusive.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use slicing notation.  
- Use positive indexes.  
- Use default values if applicable.  
- Use a single print statement.  

---  
<br>

### `Q21`
Write a code statement that prints the number of characters in `alphabet`.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use the built-in function to determine the number of characters.  

---  
<br>

### `Q22`
Write a code statement that prints the last positive index of `alphabet`.  
<br>

`Constraints`
- Use the variable `alphabet`.  
- Use the built-in function to determine the number of characters.  
- The last index must be calculated from the number of characters.  

---  
<br>
