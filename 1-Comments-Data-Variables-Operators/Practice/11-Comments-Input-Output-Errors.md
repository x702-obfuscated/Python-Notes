# `[ Comments-Input-Output-Errors : Challenges ]`

| `INSTRUCTIONS` |
|:-:|
| All assignment code should be written inside of the `main.py` file. |
| In VS Code use `CTRL + J` to open the terminal. |
| In the terminal type `python main.py` to execute your code. |
| In the terminal type `python test.py` to test your code for correctness. |


<br>

* `Preconditions` are requirements that must be true before a function is called in order for the function to operate correctly.

* `Constraints` are rules that tell you what you can or cannot do when solving a problem.

* `Requirements` are what the program is expected to be able to do. 

* `Hard coding` is the process of manually programming something that should be written programmatically.

<br>

---
<br>

# `Challenges`

---
<br>

### `Q1`

Write a single line comment that includes your full name.

<br>

Constraints:
* You must use the syntax for single line comments. 


---

<br>

### `Q2`

Write a multi-line comment with your name, class hour, and graduating class on seperate lines.

 <br>

 Constraints:
 * You must use multi-line docstring syntax

---

<br>

### `Q3`

Write a Python code statement that outputs `"Hello World!"` to the console. 

<br>

Constraints:
* The output text must match the exact text in quotes. 

---

<br>

### `Q4`

Write a Python code statement that ONLY accepts user input.

<br>

Constraints:
* The statement must ONLY accept input and perform no other action

---

<br>

### `Q5`

Write a Python code statement that prompts the user with "Enter Your Name". 


<br>
 
Constraints:
* The printed text must match the exact text in quotes. 


---

<br>

### `Q6`
Write a Python code statement that accepts input from the user and prints it to the console in the same statement.

---

<br>

### `Q7`
Write a Python code statement that prompts the user with `"Enter Your Password"`, then prints the password they entered to the console.

<br>
 
Constraints:
* The printed text must match the exact text in quotes. 

---

<br>

### `Q8`
Write a Python code statement that prints out `"Username: "`. Then on the next line write a code statement to accept user input and print it to the console. 
 
<br>
 
Constraints:
* The printed text must match the exact text in quotes. 

---

<br>

### `Q9`
Write a Python code statement that prompts the user with `"First Name: "` and then accepts input and prints it out in a seperate statement on the next line. 

On the next line write a code statement the prompts the user with `"Last Name: "` and then accepts input and prints it out in a seperate statement on the next line.

On the next line write a code statement the prompts the user with `"Date of Birth: "` and then accepts input and prints it out in a seperate statement on the next line.

<br>

Constraints:
* The prompt text must match the exact text in quotes. 

---

<br>

### `Q10`
Write a Python code statement that prints out `" 0 1 a B ! "`.

<br>

Constraints:
* The printed text must match the exact text in quotes.

---
<br>

*`Created and maintained by Mr. Merritt`*